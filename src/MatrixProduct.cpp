#include "MatrixProduct.h"

template <size_t N, size_t M>
Matrix<N, 1, double> MatrixProduct::bin_matrix_vector(Matrix<N, M, bool> m, Matrix<M, 1, double> vec){
    for (size_t i = 0; i < N; ++i){
        for (size_t j = 0; j < M; ++j){
            
        }
    }
}